import { Wrapper } from '@/modules/common/components/Wrapper';

const Home = () => {
    return (
        <Wrapper>
            <div>HOME Page</div>
        </Wrapper>
    );
};

export default Home;
