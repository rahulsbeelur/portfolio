import { Wrapper } from '../modules/common/components/Wrapper';

const Home = (): JSX.Element => {
    return (
        <Wrapper>
            <div>HOME Page</div>
        </Wrapper>
    );
};

export default Home;
